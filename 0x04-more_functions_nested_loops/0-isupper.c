#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: char to check
 *
 * Return: 0 or 1
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		Return(1);
	else
		Return(0);
}
