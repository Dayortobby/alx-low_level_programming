#include "main.h"
#include <stdio.h>
/**
 * main - Entry point
 * void _puts(char *str)
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
